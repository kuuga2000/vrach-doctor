server:
  port: ${PORT:0}
spring:
  application:
    name: doctor-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7449/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

#mvn install
#\target>java -jar doctor-service-0.0.1-SNAPSHOT.jar
#mvn spring-boot:run -D spring-boot.run.arguments="--spring.application.instance_id=uga2 --server.port=9021"
